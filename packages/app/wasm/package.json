{
  "name": "@opendaw/app-wasm",
  "private": true,
  "version": "0.1.10",
  "type": "module",
  "scripts": {
    "wasm": "node ./build-wasm.mjs",
    "predev": "npm run wasm",
    "dev": "vite --host",
    "prebuild": "npm run wasm",
    "build": "vite build",
    "preview": "vite preview --host",
    "test:rust": "node ./test-rust.mjs",
    "pretest:parity": "npm run wasm",
    "test:parity": "vitest run --config vitest.config.ts",
    "pretest:sync": "npm run wasm",
    "test:sync": "vitest run --config vitest.config.ts test/sync.test.ts",
    "test": "npm run test:rust && npm run test:parity",
    "clean": "node -e \"for (const p of ['dist', 'public/wasm']) require('node:fs').rmSync(p, {recursive: true, force: true})\""
  },
  "dependencies": {
    "@opendaw/lib-dom": "^0.0.89",
    "@opendaw/lib-dsp": "^0.0.92",
    "@opendaw/lib-fusion": "^0.0.102",
    "@opendaw/lib-jsx": "^0.0.91",
    "@opendaw/lib-std": "^0.0.84",
    "@opendaw/nam-wasm": "^1.2.0",
    "@opendaw/studio-enums": "^0.1.1",
    "jszip": "^3.10.1",
    "soundfont2": "^0.5.0"
  },
  "devDependencies": {
    "@opendaw/lib-box": "^0.0.93",
    "@opendaw/lib-runtime": "^0.0.85",
    "@opendaw/studio-adapters": "^0.3.2",
    "@opendaw/studio-boxes": "^0.0.108",
    "sass": "^1.77.0",
    "vite": "^7.0.2",
    "vitest": "^3.1.3"
  }
}
